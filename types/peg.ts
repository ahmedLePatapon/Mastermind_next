export interface PegProps {
    color: string | '#6b7280' | null
    width?: number
    height?: number
    key: number
    isEmpty?: boolean
    dashed?: boolean
};
export interface FeedbackPegProps {
    color?: string
};
